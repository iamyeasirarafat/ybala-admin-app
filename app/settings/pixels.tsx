import { Screen } from '@/components/Screen';
import { PixelForm } from '@/components/seo';

export default function PixelsScreen() {
  return (
    <Screen scroll>
      <PixelForm />
    </Screen>
  );
}
