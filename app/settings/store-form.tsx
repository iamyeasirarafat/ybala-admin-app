import { Screen } from '@/components/Screen';
import { StoreFormContainer } from '@/components/settings';

export default function StoreFormScreen() {
  return (
    <Screen scroll>
      <StoreFormContainer />
    </Screen>
  );
}
