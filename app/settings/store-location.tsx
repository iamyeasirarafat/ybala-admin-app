import { Screen } from '@/components/Screen';
import { StoreLocationManager } from '@/components/settings';

export default function StoreLocationScreen() {
  return (
    <Screen scroll>
      <StoreLocationManager />
    </Screen>
  );
}
